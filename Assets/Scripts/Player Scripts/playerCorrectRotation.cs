using UnityEngine;
using System.Collections;

public class playerCorrectRotation : GameBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
//		this.transform.LookAt(world.camera.transform.forward);
	}
}